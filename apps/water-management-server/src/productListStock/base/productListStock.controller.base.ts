/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ProductListStockService } from "../productListStock.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ProductListStockCreateInput } from "./ProductListStockCreateInput";
import { ProductListStock } from "./ProductListStock";
import { ProductListStockFindManyArgs } from "./ProductListStockFindManyArgs";
import { ProductListStockWhereUniqueInput } from "./ProductListStockWhereUniqueInput";
import { ProductListStockUpdateInput } from "./ProductListStockUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ProductListStockControllerBase {
  constructor(
    protected readonly service: ProductListStockService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ProductListStock })
  @nestAccessControl.UseRoles({
    resource: "ProductListStock",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createProductListStock(
    @common.Body() data: ProductListStockCreateInput
  ): Promise<ProductListStock> {
    return await this.service.createProductListStock({
      data: data,
      select: {
        createdAt: true,
        id: true,
        productListId: true,
        stockId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ProductListStock] })
  @ApiNestedQuery(ProductListStockFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ProductListStock",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productListStocks(
    @common.Req() request: Request
  ): Promise<ProductListStock[]> {
    const args = plainToClass(ProductListStockFindManyArgs, request.query);
    return this.service.productListStocks({
      ...args,
      select: {
        createdAt: true,
        id: true,
        productListId: true,
        stockId: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ProductListStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductListStock",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async productListStock(
    @common.Param() params: ProductListStockWhereUniqueInput
  ): Promise<ProductListStock | null> {
    const result = await this.service.productListStock({
      where: params,
      select: {
        createdAt: true,
        id: true,
        productListId: true,
        stockId: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ProductListStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductListStock",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateProductListStock(
    @common.Param() params: ProductListStockWhereUniqueInput,
    @common.Body() data: ProductListStockUpdateInput
  ): Promise<ProductListStock | null> {
    try {
      return await this.service.updateProductListStock({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          productListId: true,
          stockId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ProductListStock })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ProductListStock",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteProductListStock(
    @common.Param() params: ProductListStockWhereUniqueInput
  ): Promise<ProductListStock | null> {
    try {
      return await this.service.deleteProductListStock({
        where: params,
        select: {
          createdAt: true,
          id: true,
          productListId: true,
          stockId: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

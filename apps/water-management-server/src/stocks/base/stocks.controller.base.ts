/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { StocksService } from "../stocks.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { StocksCreateInput } from "./StocksCreateInput";
import { Stocks } from "./Stocks";
import { StocksFindManyArgs } from "./StocksFindManyArgs";
import { StocksWhereUniqueInput } from "./StocksWhereUniqueInput";
import { StocksUpdateInput } from "./StocksUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class StocksControllerBase {
  constructor(
    protected readonly service: StocksService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Stocks })
  @nestAccessControl.UseRoles({
    resource: "Stocks",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: StocksCreateInput,
  })
  async createStocks(@common.Body() data: StocksCreateInput): Promise<Stocks> {
    return await this.service.createStocks({
      data: data,
      select: {
        count: true,
        createdAt: true,
        deletedAt: true,
        documentId: true,
        id: true,
        price: true,
        productId: true,
        quantity: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Stocks] })
  @ApiNestedQuery(StocksFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Stocks",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async stocksItems(@common.Req() request: Request): Promise<Stocks[]> {
    const args = plainToClass(StocksFindManyArgs, request.query);
    return this.service.stocksItems({
      ...args,
      select: {
        count: true,
        createdAt: true,
        deletedAt: true,
        documentId: true,
        id: true,
        price: true,
        productId: true,
        quantity: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Stocks })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Stocks",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async stocks(
    @common.Param() params: StocksWhereUniqueInput
  ): Promise<Stocks | null> {
    const result = await this.service.stocks({
      where: params,
      select: {
        count: true,
        createdAt: true,
        deletedAt: true,
        documentId: true,
        id: true,
        price: true,
        productId: true,
        quantity: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Stocks })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Stocks",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: StocksUpdateInput,
  })
  async updateStocks(
    @common.Param() params: StocksWhereUniqueInput,
    @common.Body() data: StocksUpdateInput
  ): Promise<Stocks | null> {
    try {
      return await this.service.updateStocks({
        where: params,
        data: data,
        select: {
          count: true,
          createdAt: true,
          deletedAt: true,
          documentId: true,
          id: true,
          price: true,
          productId: true,
          quantity: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Stocks })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Stocks",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteStocks(
    @common.Param() params: StocksWhereUniqueInput
  ): Promise<Stocks | null> {
    try {
      return await this.service.deleteStocks({
        where: params,
        select: {
          count: true,
          createdAt: true,
          deletedAt: true,
          documentId: true,
          id: true,
          price: true,
          productId: true,
          quantity: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

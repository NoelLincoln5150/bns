/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PasswordResetTokensService } from "../passwordResetTokens.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PasswordResetTokensCreateInput } from "./PasswordResetTokensCreateInput";
import { PasswordResetTokens } from "./PasswordResetTokens";
import { PasswordResetTokensFindManyArgs } from "./PasswordResetTokensFindManyArgs";
import { PasswordResetTokensWhereUniqueInput } from "./PasswordResetTokensWhereUniqueInput";
import { PasswordResetTokensUpdateInput } from "./PasswordResetTokensUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PasswordResetTokensControllerBase {
  constructor(
    protected readonly service: PasswordResetTokensService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PasswordResetTokens })
  @nestAccessControl.UseRoles({
    resource: "PasswordResetTokens",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createPasswordResetTokens(
    @common.Body() data: PasswordResetTokensCreateInput
  ): Promise<PasswordResetTokens> {
    return await this.service.createPasswordResetTokens({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PasswordResetTokens] })
  @ApiNestedQuery(PasswordResetTokensFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PasswordResetTokens",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async passwordResetTokensItems(
    @common.Req() request: Request
  ): Promise<PasswordResetTokens[]> {
    const args = plainToClass(PasswordResetTokensFindManyArgs, request.query);
    return this.service.passwordResetTokensItems({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PasswordResetTokens })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PasswordResetTokens",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async passwordResetTokens(
    @common.Param() params: PasswordResetTokensWhereUniqueInput
  ): Promise<PasswordResetTokens | null> {
    const result = await this.service.passwordResetTokens({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PasswordResetTokens })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PasswordResetTokens",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updatePasswordResetTokens(
    @common.Param() params: PasswordResetTokensWhereUniqueInput,
    @common.Body() data: PasswordResetTokensUpdateInput
  ): Promise<PasswordResetTokens | null> {
    try {
      return await this.service.updatePasswordResetTokens({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PasswordResetTokens })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PasswordResetTokens",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePasswordResetTokens(
    @common.Param() params: PasswordResetTokensWhereUniqueInput
  ): Promise<PasswordResetTokens | null> {
    try {
      return await this.service.deletePasswordResetTokens({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

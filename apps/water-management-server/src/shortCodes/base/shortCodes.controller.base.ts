/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { ShortCodesService } from "../shortCodes.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ShortCodesCreateInput } from "./ShortCodesCreateInput";
import { ShortCodes } from "./ShortCodes";
import { ShortCodesFindManyArgs } from "./ShortCodesFindManyArgs";
import { ShortCodesWhereUniqueInput } from "./ShortCodesWhereUniqueInput";
import { ShortCodesUpdateInput } from "./ShortCodesUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ShortCodesControllerBase {
  constructor(
    protected readonly service: ShortCodesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: ShortCodes })
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createShortCodes(
    @common.Body() data: ShortCodesCreateInput
  ): Promise<ShortCodes> {
    return await this.service.createShortCodes({
      data: data,
      select: {
        consumerKey: true,
        consumerSecret: true,
        createdAt: true,
        deletedAt: true,
        displayName: true,
        enabled: true,
        id: true,
        name: true,
        registered: true,
        shortcode: true,
        typeField: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [ShortCodes] })
  @ApiNestedQuery(ShortCodesFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async shortCodesItems(@common.Req() request: Request): Promise<ShortCodes[]> {
    const args = plainToClass(ShortCodesFindManyArgs, request.query);
    return this.service.shortCodesItems({
      ...args,
      select: {
        consumerKey: true,
        consumerSecret: true,
        createdAt: true,
        deletedAt: true,
        displayName: true,
        enabled: true,
        id: true,
        name: true,
        registered: true,
        shortcode: true,
        typeField: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: ShortCodes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async shortCodes(
    @common.Param() params: ShortCodesWhereUniqueInput
  ): Promise<ShortCodes | null> {
    const result = await this.service.shortCodes({
      where: params,
      select: {
        consumerKey: true,
        consumerSecret: true,
        createdAt: true,
        deletedAt: true,
        displayName: true,
        enabled: true,
        id: true,
        name: true,
        registered: true,
        shortcode: true,
        typeField: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: ShortCodes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateShortCodes(
    @common.Param() params: ShortCodesWhereUniqueInput,
    @common.Body() data: ShortCodesUpdateInput
  ): Promise<ShortCodes | null> {
    try {
      return await this.service.updateShortCodes({
        where: params,
        data: data,
        select: {
          consumerKey: true,
          consumerSecret: true,
          createdAt: true,
          deletedAt: true,
          displayName: true,
          enabled: true,
          id: true,
          name: true,
          registered: true,
          shortcode: true,
          typeField: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: ShortCodes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteShortCodes(
    @common.Param() params: ShortCodesWhereUniqueInput
  ): Promise<ShortCodes | null> {
    try {
      return await this.service.deleteShortCodes({
        where: params,
        select: {
          consumerKey: true,
          consumerSecret: true,
          createdAt: true,
          deletedAt: true,
          displayName: true,
          enabled: true,
          id: true,
          name: true,
          registered: true,
          shortcode: true,
          typeField: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

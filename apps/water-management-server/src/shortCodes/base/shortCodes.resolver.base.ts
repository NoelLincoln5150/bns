/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ShortCodes } from "./ShortCodes";
import { ShortCodesCountArgs } from "./ShortCodesCountArgs";
import { ShortCodesFindManyArgs } from "./ShortCodesFindManyArgs";
import { ShortCodesFindUniqueArgs } from "./ShortCodesFindUniqueArgs";
import { DeleteShortCodesArgs } from "./DeleteShortCodesArgs";
import { ShortCodesService } from "../shortCodes.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ShortCodes)
export class ShortCodesResolverBase {
  constructor(
    protected readonly service: ShortCodesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "read",
    possession: "any",
  })
  async _shortCodesItemsMeta(
    @graphql.Args() args: ShortCodesCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ShortCodes])
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "read",
    possession: "any",
  })
  async shortCodesItems(
    @graphql.Args() args: ShortCodesFindManyArgs
  ): Promise<ShortCodes[]> {
    return this.service.shortCodesItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ShortCodes, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "read",
    possession: "own",
  })
  async shortCodes(
    @graphql.Args() args: ShortCodesFindUniqueArgs
  ): Promise<ShortCodes | null> {
    const result = await this.service.shortCodes(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ShortCodes)
  @nestAccessControl.UseRoles({
    resource: "ShortCodes",
    action: "delete",
    possession: "any",
  })
  async deleteShortCodes(
    @graphql.Args() args: DeleteShortCodesArgs
  ): Promise<ShortCodes | null> {
    try {
      return await this.service.deleteShortCodes(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}

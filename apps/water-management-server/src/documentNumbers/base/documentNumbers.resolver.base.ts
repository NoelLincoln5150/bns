/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DocumentNumbers } from "./DocumentNumbers";
import { DocumentNumbersCountArgs } from "./DocumentNumbersCountArgs";
import { DocumentNumbersFindManyArgs } from "./DocumentNumbersFindManyArgs";
import { DocumentNumbersFindUniqueArgs } from "./DocumentNumbersFindUniqueArgs";
import { DeleteDocumentNumbersArgs } from "./DeleteDocumentNumbersArgs";
import { DocumentNumbersService } from "../documentNumbers.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => DocumentNumbers)
export class DocumentNumbersResolverBase {
  constructor(
    protected readonly service: DocumentNumbersService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "DocumentNumbers",
    action: "read",
    possession: "any",
  })
  async _documentNumbersItemsMeta(
    @graphql.Args() args: DocumentNumbersCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [DocumentNumbers])
  @nestAccessControl.UseRoles({
    resource: "DocumentNumbers",
    action: "read",
    possession: "any",
  })
  async documentNumbersItems(
    @graphql.Args() args: DocumentNumbersFindManyArgs
  ): Promise<DocumentNumbers[]> {
    return this.service.documentNumbersItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => DocumentNumbers, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "DocumentNumbers",
    action: "read",
    possession: "own",
  })
  async documentNumbers(
    @graphql.Args() args: DocumentNumbersFindUniqueArgs
  ): Promise<DocumentNumbers | null> {
    const result = await this.service.documentNumbers(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => DocumentNumbers)
  @nestAccessControl.UseRoles({
    resource: "DocumentNumbers",
    action: "delete",
    possession: "any",
  })
  async deleteDocumentNumbers(
    @graphql.Args() args: DeleteDocumentNumbersArgs
  ): Promise<DocumentNumbers | null> {
    try {
      return await this.service.deleteDocumentNumbers(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}

/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { NotesService } from "../notes.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { NotesCreateInput } from "./NotesCreateInput";
import { Notes } from "./Notes";
import { NotesFindManyArgs } from "./NotesFindManyArgs";
import { NotesWhereUniqueInput } from "./NotesWhereUniqueInput";
import { NotesUpdateInput } from "./NotesUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class NotesControllerBase {
  constructor(
    protected readonly service: NotesService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Notes })
  @nestAccessControl.UseRoles({
    resource: "Notes",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createNotes(@common.Body() data: NotesCreateInput): Promise<Notes> {
    return await this.service.createNotes({
      data: data,
      select: {
        body: true,
        createdAt: true,
        deletedAt: true,
        id: true,
        mutable: true,
        notableId: true,
        notableType: true,
        title: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [Notes] })
  @ApiNestedQuery(NotesFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "Notes",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async notesItems(@common.Req() request: Request): Promise<Notes[]> {
    const args = plainToClass(NotesFindManyArgs, request.query);
    return this.service.notesItems({
      ...args,
      select: {
        body: true,
        createdAt: true,
        deletedAt: true,
        id: true,
        mutable: true,
        notableId: true,
        notableType: true,
        title: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Notes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Notes",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async notes(
    @common.Param() params: NotesWhereUniqueInput
  ): Promise<Notes | null> {
    const result = await this.service.notes({
      where: params,
      select: {
        body: true,
        createdAt: true,
        deletedAt: true,
        id: true,
        mutable: true,
        notableId: true,
        notableType: true,
        title: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Notes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Notes",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateNotes(
    @common.Param() params: NotesWhereUniqueInput,
    @common.Body() data: NotesUpdateInput
  ): Promise<Notes | null> {
    try {
      return await this.service.updateNotes({
        where: params,
        data: data,
        select: {
          body: true,
          createdAt: true,
          deletedAt: true,
          id: true,
          mutable: true,
          notableId: true,
          notableType: true,
          title: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Notes })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "Notes",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteNotes(
    @common.Param() params: NotesWhereUniqueInput
  ): Promise<Notes | null> {
    try {
      return await this.service.deleteNotes({
        where: params,
        select: {
          body: true,
          createdAt: true,
          deletedAt: true,
          id: true,
          mutable: true,
          notableId: true,
          notableType: true,
          title: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

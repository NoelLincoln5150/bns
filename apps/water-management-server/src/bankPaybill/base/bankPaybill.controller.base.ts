/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { BankPaybillService } from "../bankPaybill.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BankPaybillCreateInput } from "./BankPaybillCreateInput";
import { BankPaybill } from "./BankPaybill";
import { BankPaybillFindManyArgs } from "./BankPaybillFindManyArgs";
import { BankPaybillWhereUniqueInput } from "./BankPaybillWhereUniqueInput";
import { BankPaybillUpdateInput } from "./BankPaybillUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class BankPaybillControllerBase {
  constructor(
    protected readonly service: BankPaybillService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: BankPaybill })
  @nestAccessControl.UseRoles({
    resource: "BankPaybill",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: BankPaybillCreateInput,
  })
  async createBankPaybill(
    @common.Body() data: BankPaybillCreateInput
  ): Promise<BankPaybill> {
    return await this.service.createBankPaybill({
      data: data,
      select: {
        createdAt: true,
        deletedAt: true,
        enabled: true,
        id: true,
        name: true,
        paybill: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [BankPaybill] })
  @ApiNestedQuery(BankPaybillFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "BankPaybill",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankPaybills(@common.Req() request: Request): Promise<BankPaybill[]> {
    const args = plainToClass(BankPaybillFindManyArgs, request.query);
    return this.service.bankPaybills({
      ...args,
      select: {
        createdAt: true,
        deletedAt: true,
        enabled: true,
        id: true,
        name: true,
        paybill: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: BankPaybill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankPaybill",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async bankPaybill(
    @common.Param() params: BankPaybillWhereUniqueInput
  ): Promise<BankPaybill | null> {
    const result = await this.service.bankPaybill({
      where: params,
      select: {
        createdAt: true,
        deletedAt: true,
        enabled: true,
        id: true,
        name: true,
        paybill: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: BankPaybill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankPaybill",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: BankPaybillUpdateInput,
  })
  async updateBankPaybill(
    @common.Param() params: BankPaybillWhereUniqueInput,
    @common.Body() data: BankPaybillUpdateInput
  ): Promise<BankPaybill | null> {
    try {
      return await this.service.updateBankPaybill({
        where: params,
        data: data,
        select: {
          createdAt: true,
          deletedAt: true,
          enabled: true,
          id: true,
          name: true,
          paybill: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: BankPaybill })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "BankPaybill",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteBankPaybill(
    @common.Param() params: BankPaybillWhereUniqueInput
  ): Promise<BankPaybill | null> {
    try {
      return await this.service.deleteBankPaybill({
        where: params,
        select: {
          createdAt: true,
          deletedAt: true,
          enabled: true,
          id: true,
          name: true,
          paybill: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

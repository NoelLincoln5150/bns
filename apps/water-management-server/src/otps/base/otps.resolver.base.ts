/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { Otps } from "./Otps";
import { OtpsCountArgs } from "./OtpsCountArgs";
import { OtpsFindManyArgs } from "./OtpsFindManyArgs";
import { OtpsFindUniqueArgs } from "./OtpsFindUniqueArgs";
import { DeleteOtpsArgs } from "./DeleteOtpsArgs";
import { OtpsService } from "../otps.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Otps)
export class OtpsResolverBase {
  constructor(
    protected readonly service: OtpsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Otps",
    action: "read",
    possession: "any",
  })
  async _otpsItemsMeta(
    @graphql.Args() args: OtpsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Otps])
  @nestAccessControl.UseRoles({
    resource: "Otps",
    action: "read",
    possession: "any",
  })
  async otpsItems(@graphql.Args() args: OtpsFindManyArgs): Promise<Otps[]> {
    return this.service.otpsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Otps, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Otps",
    action: "read",
    possession: "own",
  })
  async otps(@graphql.Args() args: OtpsFindUniqueArgs): Promise<Otps | null> {
    const result = await this.service.otps(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Otps)
  @nestAccessControl.UseRoles({
    resource: "Otps",
    action: "delete",
    possession: "any",
  })
  async deleteOtps(@graphql.Args() args: DeleteOtpsArgs): Promise<Otps | null> {
    try {
      return await this.service.deleteOtps(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}

/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CategoryProductService } from "../categoryProduct.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CategoryProductCreateInput } from "./CategoryProductCreateInput";
import { CategoryProduct } from "./CategoryProduct";
import { CategoryProductFindManyArgs } from "./CategoryProductFindManyArgs";
import { CategoryProductWhereUniqueInput } from "./CategoryProductWhereUniqueInput";
import { CategoryProductUpdateInput } from "./CategoryProductUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CategoryProductControllerBase {
  constructor(
    protected readonly service: CategoryProductService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CategoryProduct })
  @nestAccessControl.UseRoles({
    resource: "CategoryProduct",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createCategoryProduct(
    @common.Body() data: CategoryProductCreateInput
  ): Promise<CategoryProduct> {
    return await this.service.createCategoryProduct({
      data: {
        ...data,

        productI: data.productI
          ? {
              connect: data.productI,
            }
          : undefined,
      },
      select: {
        categoryId: true,
        createdAt: true,
        id: true,

        productI: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CategoryProduct] })
  @ApiNestedQuery(CategoryProductFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CategoryProduct",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async categoryProducts(
    @common.Req() request: Request
  ): Promise<CategoryProduct[]> {
    const args = plainToClass(CategoryProductFindManyArgs, request.query);
    return this.service.categoryProducts({
      ...args,
      select: {
        categoryId: true,
        createdAt: true,
        id: true,

        productI: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CategoryProduct })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CategoryProduct",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async categoryProduct(
    @common.Param() params: CategoryProductWhereUniqueInput
  ): Promise<CategoryProduct | null> {
    const result = await this.service.categoryProduct({
      where: params,
      select: {
        categoryId: true,
        createdAt: true,
        id: true,

        productI: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CategoryProduct })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CategoryProduct",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateCategoryProduct(
    @common.Param() params: CategoryProductWhereUniqueInput,
    @common.Body() data: CategoryProductUpdateInput
  ): Promise<CategoryProduct | null> {
    try {
      return await this.service.updateCategoryProduct({
        where: params,
        data: {
          ...data,

          productI: data.productI
            ? {
                connect: data.productI,
              }
            : undefined,
        },
        select: {
          categoryId: true,
          createdAt: true,
          id: true,

          productI: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CategoryProduct })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CategoryProduct",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCategoryProduct(
    @common.Param() params: CategoryProductWhereUniqueInput
  ): Promise<CategoryProduct | null> {
    try {
      return await this.service.deleteCategoryProduct({
        where: params,
        select: {
          categoryId: true,
          createdAt: true,
          id: true,

          productI: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}

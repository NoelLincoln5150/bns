/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { TransactionCostsService } from "../transactionCosts.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { TransactionCostsCreateInput } from "./TransactionCostsCreateInput";
import { TransactionCosts } from "./TransactionCosts";
import { TransactionCostsFindManyArgs } from "./TransactionCostsFindManyArgs";
import { TransactionCostsWhereUniqueInput } from "./TransactionCostsWhereUniqueInput";
import { TransactionCostsUpdateInput } from "./TransactionCostsUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class TransactionCostsControllerBase {
  constructor(
    protected readonly service: TransactionCostsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: TransactionCosts })
  @nestAccessControl.UseRoles({
    resource: "TransactionCosts",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: TransactionCostsCreateInput,
  })
  async createTransactionCosts(
    @common.Body() data: TransactionCostsCreateInput
  ): Promise<TransactionCosts> {
    return await this.service.createTransactionCosts({
      data: data,
      select: {
        createdAt: true,
        fee: true,
        id: true,
        max: true,
        min: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [TransactionCosts] })
  @ApiNestedQuery(TransactionCostsFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "TransactionCosts",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transactionCostsItems(
    @common.Req() request: Request
  ): Promise<TransactionCosts[]> {
    const args = plainToClass(TransactionCostsFindManyArgs, request.query);
    return this.service.transactionCostsItems({
      ...args,
      select: {
        createdAt: true,
        fee: true,
        id: true,
        max: true,
        min: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: TransactionCosts })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionCosts",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async transactionCosts(
    @common.Param() params: TransactionCostsWhereUniqueInput
  ): Promise<TransactionCosts | null> {
    const result = await this.service.transactionCosts({
      where: params,
      select: {
        createdAt: true,
        fee: true,
        id: true,
        max: true,
        min: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: TransactionCosts })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionCosts",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: TransactionCostsUpdateInput,
  })
  async updateTransactionCosts(
    @common.Param() params: TransactionCostsWhereUniqueInput,
    @common.Body() data: TransactionCostsUpdateInput
  ): Promise<TransactionCosts | null> {
    try {
      return await this.service.updateTransactionCosts({
        where: params,
        data: data,
        select: {
          createdAt: true,
          fee: true,
          id: true,
          max: true,
          min: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: TransactionCosts })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "TransactionCosts",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteTransactionCosts(
    @common.Param() params: TransactionCostsWhereUniqueInput
  ): Promise<TransactionCosts | null> {
    try {
      return await this.service.deleteTransactionCosts({
        where: params,
        select: {
          createdAt: true,
          fee: true,
          id: true,
          max: true,
          min: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
